FROM node:22-alpine

RUN apk add --no-cache python3 make g++ \
    && npm install -g obsidian-headless \
    && apk del python3 make g++

RUN mkdir -p /vault

VOLUME ["/vault", "/root/.config/obsidian-headless"]

ENTRYPOINT ["ob"]
CMD ["sync", "--continuous", "--path", "/vault"]
